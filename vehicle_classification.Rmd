---
title: "Vehicle Classification in Norway"
output: html_notebook
---


```{r setup, include = FALSE, echo = FALSE}
# Packages
library(tidyverse)
library(jsonlite)
library(flextable)
library(httr)
library(writexl)

# knitr options
knitr::opts_chunk$set(echo = FALSE,
                      warning = FALSE,
                      message = FALSE,
                      error = FALSE)

source("get_from_data_norge.R")
```

Here comes something...
```{r getdata}
# Must have four data sets

# All (61 rows)
vehicle_groups <- get_vehicle_groups()

vehicle_technical_codes <- get_technical_codes()

vehicle_info_fields <- get_vehicle_info_fields()

vehicle_info <- get_vehicle_info()

writexl::write_xlsx(vehicle_info, path = "lastebiler_over_12_tonn.xlsx")

 # %>%
 #      dplyr::select(tekn_aksler, tekn_aksler_drift, tekn_avreg_dato, tekn_egenvekt, tekn_kjtgrp,
 #                    tekn_lengde, tekn_merke, tekn_reg_aar, tekn_reg_status,
 #                    tekn_totvekt, tekn_vogntogvekt
 #      )


# Except trailers and beltebil (1e5 rows)


# Limited to still in use (4.4e6 rows)
vehicles_of_approved_type

# Limited to still in use (1e6 rows) and no trailers
vehicles_approved_individually


```

